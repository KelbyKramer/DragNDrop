<script src="/app/assets/javascripts/tasks.js"></script>

<p id="notice"><%= notice %></p>



<h1>Tasks</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Info</th>
      <th>Postion</th>
      <th colspan="3"></th>
    </tr>
  </thead>
<!--.order(:position) 
<% #@tasks.sort{|a,b| a.position <=> b.position}.each do |task| %>
 <% #@tasks.each do |task| %>
 <% #end %>
 #this one doesn't throw an error:
 #    <%# @tasks.sort{|a,b| a[:position] <=> b[:position]}.each do |task| %>

 -->
 
  <tbody>
     <% @tasks.each do |task| %>
      <tr>
        <td><%= task.name %></td>
        <td><%= task.info %></td>
        <td><%= task.postion %></td>
        <td><%= link_to 'Show', task %></td>
        <td><%= link_to 'Edit', edit_task_path(task) %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Task', new_task_path %>
<br>
<br>
<%= @tasks %>
<br>

<br>

<p>Click the button to remove the last element from the array.</p>

<button onclick="myFunction2()">Try it</button>

<p id="demo"></p>
<script>
var fruits = ["Banana", "Orange", "Apple", "Mango"];
document.getElementById("demo").innerHTML = fruits;


</script>

<%= javascript_tag do %>
  
  dataForJs = <%= raw @tasks.to_json %>
<% end %>



<button onclick="increment_position(dataForJs)">Increment position</button>
<br>
<%= @tasks[1].postion %>
